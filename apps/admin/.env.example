# Backend API URL (baked into bundle at build time)
VITE_API_URL=http://localhost:8080

# Google OAuth Web Client ID (baked into bundle at build time)
# Create a Web Application OAuth Client ID in Google Cloud Console
# Add authorized JavaScript origins: http://localhost:3000
VITE_GOOGLE_CLIENT_ID=your_google_web_client_id_here
