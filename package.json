{
  "name": "alacarte",
  "version": "1.0.0",
  "private": true,
  "description": "A la carte - Multi-platform rating and sharing system for consumables",
  "scripts": {
    "build": "turbo build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "turbo": "^1.11.2"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
